[module/checkupdates]
type = custom/script

interval = 600
exec = /home/iwancof/.config/polybar/modules/checkupdates.py

label = %output%
label-fail = failed to collect data
format = <label>

format-foreground = ${colors.bar-foreground}
