[module/network]
type = custom/script

interval = 10
exec = /home/iwancof/.config/polybar/modules/network.py

label = %output%
label-fail = failed to collect data
format = <label>

format-foreground = ${colors.bar-foreground}
